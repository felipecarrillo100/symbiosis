import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hexagon.symbiosis',
  appName: 'symbiosis',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
